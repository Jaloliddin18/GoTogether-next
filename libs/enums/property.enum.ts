// STUB — property module removed from backend
// Kept only to prevent TypeScript build errors in legacy pages
// TODO: remove when legacy property pages are fully transformed

export enum PropertyType {
	APARTMENT = 'APARTMENT',
	HOUSE = 'HOUSE',
	VILLA = 'VILLA',
	STUDIO = 'STUDIO',
}

export enum PropertyStatus {
	ACTIVE = 'ACTIVE',
	SOLD = 'SOLD',
	DELETE = 'DELETE',
}

export enum PropertyLocation {
	SEOUL = 'SEOUL',
	BUSAN = 'BUSAN',
	INCHEON = 'INCHEON',
	DAEGU = 'DAEGU',
}
